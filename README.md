# portfolio

A personal portfolio website with projects, experience, and live stats.

**Status:** Development complete.

**LeetCode stats (optional)**
Run `npm run update:facts` to refresh `public/data/facts.json` using `scripts/update-facts.mjs` after setting your LeetCode username. Automate with cron or GitHub Actions if you want scheduled updates.
