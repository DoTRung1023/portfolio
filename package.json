{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "dependencies": {
    "express": "^5.1.0"
  },
  "scripts": {
    "dev": "node src/server.mjs",
    "start": "node src/server.mjs",
    "update:facts": "node --env-file=.env scripts/update-facts.mjs 2>&1 | grep -v 'injected env'"
  }
}
